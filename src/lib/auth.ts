'use server'

// Simulate an auth check (replace with real logic later)
export async function isUserAuthenticated() {
  // In real app, check session or JWT
  return false; // Change to `true` to simulate logged-in user
}
